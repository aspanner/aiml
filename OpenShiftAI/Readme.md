This is my bare bones approach to get OpenShiftAI (including it's legacy naming conventions of RHODS) deployed onto a fresh OCP cluster.
In order to use just do a `oc apply -f <filename in ascending order>`
